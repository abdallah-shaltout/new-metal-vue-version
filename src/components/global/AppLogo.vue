<template>
    <div class="flex-center gap-4">
        <img src="/logo.png" class="w-14 h-14" />
        <div class="flex-center flex-col">
            <h1 class="font-bold text-gray-700 text-xl">{{ $t('header.name') }}</h1>
            <span class="text-sm text-dark">{{ $t('header.name-description') }}</span>
        </div>
    </div>
</template>
